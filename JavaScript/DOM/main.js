var cajas = document.getElementsByName('div');

function agregaCaja(texto)
{
    var nuevo= document.createElement('div');
}

// var entrada= document.getElementById('entrada');  // es para acceder a un objeto 
// var contenido = entrada.value;           obtiene el valor del objeto
// agregaCaja(contenido);

 /*event handler   
      Es unico  solo puede llamar a una funcion 
*/

 /* Event listener 
      Se puede llamar muchas veces a una funcion
 */